package offgrid.geogram.wifi.comm;

public enum VisibilityType {
    PUBLIC,     // anyone can see it
    PRIVATE,    // only the author can see it
    RESTRICTED  // only groups of specified user can see
}
